# Архитектура — ИИ-менеджер (Wazzup + MAX)

Мебельная компания на заказ. Бот ведёт только **новые** чаты:
сообщения читает и пишет через **Wazzup API**, запрос цены — в группу **MAX**.

**amoCRM сейчас в коде не участвует** — подключим отдельно позже.

Опора на соседние проекты:
- диалог / маркеры / батчер / follow-up — `Vladimir_mebel`
- gatekeeper «только новые чаты» — `mebel_Iliya` / `Karina`
- фотопоиск — `mebel_Iliya` / `demo_for_mebel` (DINOv2 + Qdrant)
- MAX — `lead_magnit` / `connect_hub`
- webhook-паттерн — `msd` (Wazzup API)

---

## 1. Роли сейчас

```
Мессенджеры (WhatsApp, Avito, TG, …)
        ↓
     Wazzup API     ← бот СЛУШАЕТ и ОТВЕЧАЕТ (есть API)
        ↓
  Наш ИИ-сервис
        ↓ (цена + Авито show-phone)
     MAX-группа
```

| Система | Что делаем сейчас |
|---------|-------------------|
| **Wazzup** | Входящие, ответы, фото. Событие Авито «показ номера» ловим здесь. |
| **MAX** | Цена (intent) + Авито «посмотрел номер, не позвонил». |
| **amoCRM** | Вне кода. Позже. |

```
Клиент пишет / Авито шлёт системное событие
        → Wazzup
        → наш сервис
        → Gatekeeper
        → ответ через Wazzup API
```

Сервис мониторит чаты в Wazzup, пишет только в **новые**.

---

## 2. Pull / Push и тихие часы

| Режим | Что | Когда |
|-------|-----|-------|
| **Pull** | Клиент написал → отвечаем | Всегда |
| **Push** | Сами пишем (напоминание 4ч) | Только **09:00–18:00 МСК** |

Тихие часы **18:00→09:00 МСК**: push откладываем, pull можно.

---

## 3. Gatekeeper

| Статус | Смысл | Бот |
|--------|--------|-----|
| `new` | Первый контакт | Ведёт |
| `existing` | Чат был до бота | Молчит |
| `manual` | Менеджер / `#стоп` | Молчит до `#старт` |

---

## 4. Поток

```
Wazzup webhook / API
  → дедуп → gatekeeper → batcher
  → Dialog (Intent + Manager, history=40)
       ├─ текст / фото (Vision)
       ├─ intent «ждёт цену» → MAX
       └─ Авито show-phone → только в MAX (клиенту не пишем)
  → ответ через Wazzup
```

---

## 5. Сценарии

### Текст / фото
Ответ и карточки через Wazzup.

### Цена
Intent «ждёт цену» → MAX → владелец пишет цену → клиенту as-is в Wazzup.

### Авито «показ номера»
В Wazzup видно: клиент посмотрел номер на Авито и не позвонил.
Ловим событие из Wazzup → **только в группу MAX**. Клиенту не отвечаем.

### Клиент пропал
4 часа → напоминание в Wazzup (09–18 МСК).

---

## 6. Модули

```
app/transports/wazzup.py   # I/O + детект Avito show-phone
app/core/                  # gatekeeper, batcher, dialog, markers
app/vision/                # DINOv2 + Qdrant (4807 фото)
app/notify/max.py          # цена + Авито show-phone
app/jobs/price_relay.py
app/jobs/followups.py
app/jobs/avito_show_phone.py
```

---

## 7. Позже

1. Ключи Wazzup / сессия MAX  
2. amoCRM (сделки, стадии) — отдельным этапом  
3. Точный текст/тип события Avito show-phone в живом Wazzup  
4. Сценарии заказчика  
