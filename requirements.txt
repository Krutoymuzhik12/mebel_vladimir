fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0
openpyxl>=3.1.0
pillow>=10.0.0
tzdata>=2024.1
# PDF → текст для модели (бесплатно). DOCX опционально: pip install python-docx
pypdf>=5.0.0
# Vision (отдельный venv, как demo_for_mebel):
# torch
# qdrant-client
# rembg
# MAX (позже):
# maxapi-python / pymax
# Голос — код готов, ставится один из провайдеров:
#   pip install faster-whisper     (local, бесплатно, ~1 ГБ RAM на модель small)
#   pip install openai             (OPENAI_API_KEY)
#   pip install fastapi-poe        (poe, использует уже имеющийся POE_API_KEY)
